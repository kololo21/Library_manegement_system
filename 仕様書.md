# 図書館管理システム — 仕様書

## 0.コンパイル方法
Get-ChildItem -Path src -Recurse -Filter "*.java" | Select-Object -ExpandProperty FullName > sources.txt
javac -d out (Get-Content sources.txt)
java -cp out Main

## 1. 概要

図書館スタッフが「本」と「会員」の情報を管理し、貸出・返却操作を行うデスクトップアプリケーション。

| 項目 | 内容 |
|------|------|
| 言語 | Java（Java SE のみ、外部ライブラリなし） |
| UI | Swing（GUI） |
| データ永続化 | Java Serialization（`.dat` ファイル） |
| 対象ユーザー | 図書館の係員 |

---

## 2. 管理するデータ

### 2.1 本（Book）

| フィールド | 型 | 説明 |
|---|---|---|
| `bookId` | String | 自動採番（例: `B0001`） |
| `title` | String | タイトル |
| `author` | String | 著者名 |
| `genre` | String | ジャンル（例: Fiction, Science, History…） |
| `totalCopies` | int | 所有冊数 |
| `lentCopies` | int | 貸し出し中の冊数 |

派生情報:
- 在庫冊数 = `totalCopies − lentCopies`
- 貸出可否 = `lentCopies < totalCopies`

### 2.2 会員（Member）

| フィールド | 型 | 説明 |
|---|---|---|
| `memberId` | String | 自動採番（例: `M0001`） |
| `name` | String | 氏名 |
| `email` | String | メールアドレス |
| `phone` | String | 電話番号 |

### 2.3 貸出記録（LoanRecord）

| フィールド | 型 | 説明 |
|---|---|---|
| `loanId` | String | 自動採番（例: `L0001`） |
| `bookId` | String | 本の参照ID |
| `memberId` | String | 会員の参照ID |
| `loanDate` | LocalDate | 貸出日 |
| `dueDate` | LocalDate | 返却期限（貸出日 + 14日） |
| `returnDate` | LocalDate / null | 実際の返却日（未返却は null） |
| `status` | enum | `ACTIVE`（貸出中）/ `RETURNED`（返却済）/ `OVERDUE`（延滞） |

---

## 3. 機能要件

### 3.1 必須機能（基本点）

#### 本の管理
- 本の追加
- 本の情報編集（タイトル・著者・ジャンル・冊数）
- 本の削除（貸出中の場合は削除不可）
- 本の一覧表示（テーブル形式）
- 本の詳細表示（現在誰が借りているか）

#### 会員の管理
- 会員の追加
- 会員情報の編集
- 会員の削除（貸出中の場合は削除不可）
- 会員の一覧表示（テーブル形式）
- 会員の詳細表示（現在借りている本の一覧）

#### 貸出操作
- 本の貸し出し（会員を選択 → LoanRecord 作成）
- 本の返却（LoanRecord をクローズ、返却日を記録）

### 3.2 +α機能（加点対象）

| 機能 | 内容 |
|---|---|
| **貸出履歴管理** | 過去すべての貸出記録を閲覧・フィルタリング |
| **返却期限管理** | 14日間の貸出期間を表示 |
| **延滞警告** | 延滞中の貸出を赤くハイライト、ダッシュボードに件数表示 |
| **検索・フィルタ** | 本をタイトル・著者・ジャンルで検索、会員を氏名で検索 |
| **統計ダッシュボード** | 総冊数・会員数・貸出中件数・延滞件数を一覧表示 |
| **おすすめ本3冊** | 会員の過去の貸出ジャンルをもとに、未読の本を3冊推薦 |

---

## 4. 非機能要件

- コード・コメント・UI テキストはすべて**英語のみ**（課題の制約）
- 外部ライブラリは使用しない（Java SE のみ）
- データは書き込みのたびに自動保存
- データファイルが存在しない場合（初回起動）は空の状態で正常起動

---

## 5. 画面構成（Swing）

```
MainFrame（JFrame）
├── メニューバー
│   ├── File → Save, Exit
│   └── Help → About
├── JTabbedPane（タブ）
│   ├── ダッシュボードタブ
│   │   ├── 統計パネル（総冊数・会員数・貸出中・延滞件数）
│   │   └── 延滞中貸出テーブル
│   ├── 本タブ
│   │   ├── 検索バー + ジャンルフィルタ
│   │   ├── 本テーブル（JTable）
│   │   └── ボタン：追加 / 編集 / 削除 / 貸し出し / 詳細
│   ├── 会員タブ
│   │   ├── 検索バー
│   │   ├── 会員テーブル（JTable）
│   │   └── ボタン：追加 / 編集 / 削除 / 詳細
│   └── 貸出履歴タブ
│       ├── フィルタ：全件 / 貸出中 / 返却済 / 延滞中
│       └── 貸出履歴テーブル
└── ステータスバー（最下部）
```

ダイアログ（JDialog）:
- `BookFormDialog` — 本の追加・編集
- `MemberFormDialog` — 会員の追加・編集
- `LendDialog` — 貸し出す会員を選択
- `BookDetailDialog` — 現在の借用者 + おすすめ本
- `MemberDetailDialog` — 現在の貸出 + 貸出履歴

---

## 6. データ保存先

```
data/
├── books.dat       ← List<Book>
├── members.dat     ← List<Member>
└── loans.dat       ← List<LoanRecord>
```

- すべてのモデルクラスは `Serializable` を実装
- `DataManager` クラスが読み書きを一元管理

---

## 7. おすすめ本のロジック

1. 対象会員の過去の貸出記録からジャンルを集計
2. そのジャンルに属する本の中で、**その会員がまだ借りたことのない本**を抽出
3. 在庫冊数が多い順に並べる
4. 上位3冊を推薦

※貸出履歴がない会員には、在庫冊数が最も多い本を3冊推薦
